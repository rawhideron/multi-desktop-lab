# multi-desktop-lab
Docker-compose file and dockerfiles to start 6 linux distributions with desktop and rdp connections


# Build everything
docker-compose build

# Start all 6 desktops
docker-compose up -d


# add a line to shoe how you perform build for only one container
